# VoxelSwarm — Environment Configuration
# Copy to .env and edit values.
# Most settings are stored in the SQLite database (settings table).
# This file handles only pre-database bootstrap values.

APP_ENV=production
APP_DEBUG=false
